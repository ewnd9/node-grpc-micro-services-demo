{
  "name": "@internal/cats-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && tooling build-proto"
  },
  "dependencies": {
    "@internal/tooling": "0.0.0"
  }
}
