{
  "name": "@internal/cats",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start:dev": "ts-node ./src",
    "build": "rm -rf dist && tsc",
    "build:turbo": "tooling turbo-build",
    "build:docker": "tooling turbo-docker"
  },
  "dependencies": {
    "@internal/cats-api": "0.0.0",
    "@internal/grpc-server": "0.0.0"
  }
}
